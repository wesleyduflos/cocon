import { ComingSoon } from "@/components/shared/coming-soon";

export default function CalendarPage() {
  return (
    <ComingSoon
      emoji="📅"
      title="L'agenda se prépare"
      sprintHint="Mini-mois + jour sélectionné, sync Google Calendar — au sprint 2."
    />
  );
}
